using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using Store.Models;

namespace Store.Classes
{
    public static class UtilsHtmlTag
    {
        //public static string CreateSelectTag(
        //        IEnumerable<SelectListModel> items, 
        //        string itemAttr/*,
        //        int selectedValue*/)
        //{
        //    var res = "<select class=\"form-control col-md-3\" id=" + itemAttr + ">";

        //    foreach (var id in items)
        //    {
        //        res = res + "<option value=" + id.Id + " " + (id.Id == selectedValue ? "selected" : string.Empty) + ">" + id.Text + "</option>";
        //    }
        //    res = res + "</select>";

        //    return res;
        //}
    }
}